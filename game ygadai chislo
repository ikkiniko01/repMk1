//Прикольная программа 

#include <iostream>
#include <random>
#include <limits> // для std::numeric_limits

using namespace std;

// Функция для получения игрока цифры
int getPlayerGuess() {
    int guess;
    while (true) {
        cout << "Введите ваше предположение: ";
        cin >> guess;

        if (cin.fail()) { // Проверка на некорректный ввод
            cin.clear(); // Сброс состояния
            cin.ignore(numeric_limits<streamsize>::max(), '\n'); // Удаление некорректного ввода
            cout << "Пожалуйста, введите число!" << endl;
        } else {
            cin.ignore(numeric_limits<streamsize>::max(), '\n'); // Удаление лишних символов из буфера
            return guess;
        }
    }
}

int main() {
    random_device rd;
    mt19937 gen(rd());
    uniform_int_distribution<> distrib(1, 100);   

    int secretNumber = distrib(gen); // Загаданное число от 1 до 100
    int playerGuess = 0;
    int attempts = 0;

    cout << "Добро пожаловать в игру 'Угадай число'!" << endl;
    cout << "Я загадал число от 1 до 100. Попробуй его угадать!" << endl;

    while (playerGuess != secretNumber) {
        playerGuess = getPlayerGuess();
        attempts++;

        if (playerGuess < secretNumber) {
            cout << "Слишком маленькое число. Попробуйте снова." << endl;
        } else if (playerGuess > secretNumber) {
            cout << "Слишком большое число. Попробуйте снова." << endl;
        } else {
            cout << "Поздравляю! Вы угадали число " << secretNumber << " за " << attempts << " попыток." << endl;
        }
    }

    return 0;
}
